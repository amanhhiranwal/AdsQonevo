{
  "name": "adsqonevo-app",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack Next.js lead capture app with Express and PostgreSQL",
  "scripts": {
    "dev": "npm run dev:all",
    "dev:all": "npx concurrently -n \"server,client\" -c \"blue,green\" \"npm run dev:server\" \"npm run dev:client\"",
    "dev:client": "npm --prefix client run dev",
    "dev:server": "npm --prefix server run dev",
    "build:client": "npm --prefix client run build",
    "start:client": "npm --prefix client run start",
    "start:server": "npm --prefix server run start",
    "setup": "npm --prefix server install && npm --prefix client install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
